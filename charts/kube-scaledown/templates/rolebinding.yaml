apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kube-scaledown.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kube-scaledown.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kube-scaledown.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "kube-scaledown.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
