---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: downscaleschedules.downscaler.sipher.gg
spec:
  group: downscaler.sipher.gg
  names:
    kind: DownscaleSchedule
    listKind: DownscaleScheduleList
    plural: downscaleschedules
    shortNames:
    - ds
    singular: downscaleschedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.currentState
      name: State
      type: string
    - jsonPath: .status.managedResources
      name: Managed
      type: integer
    - jsonPath: .status.scaledDownResources
      name: Scaled Down
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DownscaleSchedule is the Schema for the downscaleschedules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DownscaleScheduleSpec defines the desired schedule configuration.
            properties:
              downtimeReplicas:
                default: 0
                description: 'Replica count during downtime (default: 0)'
                format: int32
                minimum: 0
                type: integer
              excludeNamespaces:
                description: Namespaces to exclude
                items:
                  type: string
                type: array
              excludeResources:
                description: Individual resources to exclude
                items:
                  description: ResourceRef identifies a specific resource to exclude.
                  properties:
                    kind:
                      description: Resource kind (e.g., "Deployment", "Fleet")
                      type: string
                    name:
                      description: Resource name
                      type: string
                    namespace:
                      description: Resource namespace
                      type: string
                  required:
                  - kind
                  - name
                  - namespace
                  type: object
                type: array
              includeNamespaces:
                description: Namespaces to include (empty = all namespaces)
                items:
                  type: string
                type: array
              includeResources:
                description: Resource types to manage (e.g., "deployments", "statefulsets",
                  "fleets.agones.dev")
                items:
                  type: string
                minItems: 1
                type: array
              uptime:
                description: Uptime schedule in format "Mon-Fri 08:00-20:00 Asia/Ho_Chi_Minh"
                type: string
            required:
            - includeResources
            - uptime
            type: object
          status:
            description: DownscaleScheduleStatus defines the observed state.
            properties:
              currentState:
                description: 'Current state: "uptime" or "downtime"'
                type: string
              lastScaleDown:
                description: Last time resources were scaled down
                format: date-time
                type: string
              lastScaleUp:
                description: Last time resources were scaled up
                format: date-time
                type: string
              managedResources:
                description: Number of resources managed by this schedule
                format: int32
                type: integer
              scaledDownResources:
                description: Number of resources currently scaled down
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
